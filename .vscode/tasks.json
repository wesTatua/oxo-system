{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "OXO: Install & Start Expo (tunnel)",
      "type": "shell",
      "command": "(pkill -f 'expo start' || true) && npm install && npx expo install --fix && npx expo start --tunnel --port 8082",
      "group": { "kind": "build", "isDefault": true },
      "presentation": {
        "reveal": "always",
        "panel": "new"
      },
      "problemMatcher": []
    }
  ]
}
